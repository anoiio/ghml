id: strict
version: "0.1"
description: >
  Strict policy for shared/public demos.
  Only fast/cheap model; tight request and token caps.

model_allowlist:
  - claude-haiku-4-5

cost_caps:
  max_tokens_per_request: 1024
  max_requests_per_session: 5
  max_total_tokens_per_session: 5000
